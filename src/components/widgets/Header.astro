<style is:global>
  /* Style cho container Google Dịch */
  #google_translate_element {
    /* XÓA: position: fixed, top, right */
    position: relative; /* Để nó nằm theo dòng chảy văn bản */
    z-index: 50;
    /* Điều chỉnh lại padding cho gọn */
    padding: 0; 
    background: transparent;
    box-shadow: none;
    font-size: 14px;
    /* Đảm bảo chiều cao khớp với header */
    display: flex;
    align-items: center;
  }

  /* Ẩn icon G logo của Google để gọn hơn */
  .goog-te-gadget-icon {
    display: none !important;
  }

  /* Chỉnh lại font và màu sắc trong dropdown cho khớp theme */
  .goog-te-gadget-simple {
    background-color: transparent !important;
    border: 1px solid rgba(128, 128, 128, 0.2) !important;
    padding: 4px 8px !important;
    border-radius: 4px !important;
    font-size: 0.875rem !important;
    line-height: 1.25rem !important;
    display: flex !important;
    align-items: center !important;
  }

  /* Chữ "Select Language" hoặc ngôn ngữ đang chọn */
  .goog-te-menu-value span {
    color: #374151 !important; /* Màu xám đậm */
    font-weight: 500 !important;
  }
  
  /* Hỗ trợ Dark mode (nếu AstroWind đang bật dark mode class ở html tag) */
  :global(.dark) .goog-te-menu-value span {
    color: #e5e7eb !important; /* Màu trắng xám cho dark mode */
  }

  /* Ẩn thanh Banner "Translate" hiện lên trên đầu trang */
  body {
    top: 0px !important;
  }
  .goog-te-banner-frame {
    display: none !important;
  }
</style>
